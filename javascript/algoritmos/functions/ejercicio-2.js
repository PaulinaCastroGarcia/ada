// Escribir una función que dado un número X (base) y un número N (potencia),
// muestre por consola el resultado de realizar X^N (X elevado a la N).

// function elevado(x, n){
// 	var cuenta = 1;
// 	for (i = 0; i < n; i++){
// 		cuenta = cuenta * x ;
// 	}
// 	return cuenta;
// }

// elevado(3,3)

function elevado(x,n) {
	var xn = x ** n;
	return xn
}

elevado(3,3)

