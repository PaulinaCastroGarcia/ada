// Mostrar todos los números de 1 a N 
//aumentando de 2 en 2 donde n lo ingresa el usuario en un prompt.

var n = prompt('ingrese un numero');

for (var i = 1; i <= n; i = i + 2 ) {
	console.log(i);
}